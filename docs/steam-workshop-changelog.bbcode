[h1]Changelog[/h1]












[h2]v0.25.0[/h2]
[list]
[*] receiver quality — radio hardware affects WBN broadcast degradation
[*] tutorial expansion Phase 1 — 8 new milestones for critical gaps
[*] broadcast influence Phase A — perceived pressure + trust mutation
[*] entropy Phase 3 — seasonal, shadows, speculation, trust, desperation
[*] entropy Phase 2 — weather, blackout, wholesaler concealment
[*] implement entropy system Phase 1 — fog-of-market layer (v0.24.0)
[*] wire event→market pricing + fragment→MarketDatabase bridge
[*] (Fix) use vanilla HasPlayerInRange() for WBN proximity gate
[*] (Fix) gate WBN broadcasts by device-to-player distance
[*] (Fix) add WMD_BROADCAST_INFLUENCE to data reset service
[*] (Fix) add missing id field to fragment→MarketDatabase records
[*] (Fix) add Definitions/TextPools/ prefix to voice pack require paths
[*] (Fix) handle string confidence tiers in entropy recordObservation
[*] (Fix) wire WBN radio pipeline — 3 integration bugs + magic number extraction
[*] update roadmap — signal ecology v2 done, receiver quality, branch status
[*] update roadmap — entropy system fully complete (all 3 phases)
[*] comprehensive roadmap update reflecting current state (v0.23.0)
[*] add entropy system design doc + market entropy doctrine (DG 59)
[/list]

[hr][/hr]

[h2]v0.23.0[/h2]
[list]
[*] radio proximity filtering for intelligence intercepts (v0.23.0)
[*] expand POSnet item spawn locations + SIGINT skill books
[*] new Automotive + Agriculture core categories + fix duplicate keys
[*] property-bonus pricing for uncurated items (§58 Tier 3)
[*] three-tier item pricing — packaging detection + 51 curated overrides (§58)
[*] event-driven live refresh for 6 terminal screens (§57)
[*] Tier V Strategic Relay — remote terminal control
[*] Passive Recon Signal Ecology + Active Satellite Scanning
[*] Satellite Broadcast Screen + Network Hub (Tier IV Phase 5)
[*] WBN Operations Net — 3rd radio channel (Feature B)
[*] Signal Fragment Review Screen (Feature A)
[*] (Fix) add missing UI_POS_Tiles translation key
[*] (Fix) buy action error feedback + safecall return value handling
[*] (Fix) property-bonus pricing CTD — pcall cannot catch Java method dispatch
[*] (Fix) CommodityItems destroy crashes on undefined _refreshListeners
[*] (Fix) Tier IV/V coverage gaps + commodity items +/- regression
[*] (Fix) Satellite Scan accepts Tier V relay calibration
[*] (Fix) 4 Commodity Items screen issues
[*] (Fix) replace 14 calls to non-existent refreshCurrentScreen()
[*] (Fix) data reset coverage — add 8 missing stores + prefix cleanup
[*] (Fix) relay wiring — multi-Z desktop search + remote data link
[*] (Fix) Tier V relay wiring detection — safecall destructuring + key bridging
[*] (Fix) verify Tier V relay sprites against PZ Build 42 map data
[*] (Fix) Tier V relay detection uses large dish sprites, not small
[*] (Fix) Satellite Scan back button crash — back() vs goBack()
[*] (Fix) replace hasTag with getFullType for artifact scanning
[*] (Fix) hasTag crash on Clothing items in artifact scanner
[*] (Fix) remove 3 duplicate translation keys causing JSON parse crash
[*] (Fix) CommodityDetail ContextPanel crash on nil summary
[*] (Fix) 3 UI bugs — fragment keys, PN zone names, signal day/type
[*] (Fix) voice pack integration + kHz→MHz + fragment key mismatches
[*] (Fix) PN notification zone names not resolving (snake_case vs PascalCase)
[*] sandbox cleanup — remove 5 always-on options, add relay range
[*] add JSON duplicate key check for translation files
[*] wire Operations Net body text through voice pack registry
[/list]

[hr][/hr]

[h2]v0.22.0[/h2]
[list]
[*] Signal Ecology polish — 5 fixes + docs
[*] Signal Ecology v2 — multi-pillar environmental signal model
[*] WBN forecast system + terminal band restriction
[*] WBN content expansion — weather, power, voice packs, signal fragments
[*] ambient broadcast generation + market category translation keys
[*] register WBN broadcast channels with AZAS Frequency Index
[*] WBN radio voices for all 9 archetypes
[*] integrate WBN composition with Voice Pack system
[*] World Broadcast Network Phase 1 MVP
[*] (Fix) rebalance Signal Ecology clarity values — composite too low
[*] (Fix) convert Signal Modifier definitions to return-array pattern
[*] (Fix) Signal Ecology safecall misuse — __lt not defined crash
[*] (Fix) add SandboxVars to luacheckrc read_globals
[*] (Fix) 3 WBN translation key mismatches causing text bleed-through
[*] (Fix) WBN broadcasts go silent after first emission
[*] (Fix) WBN broadcasts never transmitting — 3 root causes
[*] (Fix) wholesaler spawning blocked by .entries metadata false positive
[*] (Fix) move DynamicRadio from read_globals to globals (read-write)
[*] (Fix) add DynamicRadio to luacheck globals
[*] (Fix) Lua 5.1 compat + resilient AZAS lookup in WBN services
[*] (Fix) Market Signals ??? zone/categories for event-sourced rumours
[*] (Fix) ContextPanel never renders — getContextData missing from legacy screen
[*] (Fix) View Details selection lost during screen refresh
[*] (Fix) View Details crash + AgentDeploy screen not loading
[*] (Fix) contract generation + standalone agent deploy + signals crash
[*] clarity-by-tier rebalance rationale + tuning table
[*] WBN content expansion — weather, power, fragments, voice packs
[*] WBN Phase 1 coding conventions + player guide + impl notes
[*] World Broadcast Network + Radio Band Taxonomy design
[*] integrate Tier V Dish / Signal Ecology / Broadcast Influence design
[*] Tier V Strategic Relay, Signal Ecology, broadcast influence layer
[*] remove all Dynamic Trading references
[*] Living Market always enabled, remove sandbox toggle
[*] docs+fix: data stores reference doc + reset coverage for all 28 stores
[*] debug: add ContextPanel render diagnostics
[/list]

[hr][/hr]

[h2]v0.21.0[/h2]
[list]
[*] §49 No Silent Gates — band/SIGINT/signal/difficulty hints
[*] CI translation key reference audit
[*] voiced free agent state messages — 5 archetypes × 9 states
[*] voice pack expansion — schema + data-pack + 8 submission pools
[*] ContextPanel for 5 screens + hardcoded strings → keys + crafter voice
[*] Market Signals filter tabs + ContextPanel detail + deprecations
[*] DualTab refactor for Assignments + Starlit event listeners
[*] absorb Wholesaler Directory into Contacts as Tab 2 (23→22 screens)
[*] consolidate Market Overview into 3-tab dashboard (25→23 screens)
[*] agent visibility + risk model + DualTab + signal feed
[*] Deploy UI + cargo consumption + contract settlement for free agents
[*] Free Agent screen — dual tab bars (archetype × status)
[*] Free Agent System — schema, service, state machine, economy tick
[*] 2 new terminal screens — Wholesaler Directory + Zone Overview
[*] cross-mod missions — PCP chemistry supply + PIP specimen collection
[*] repeat visit discount — 50% faster intel gathering at known sites
[*] spot sell on CommodityItems — Buy/Sell mode toggle
[*] signal strength affects mission quality — difficulty cap + briefing garble
[*] (Fix) CI translation audit — skip keys ending with _ (concatenation prefixes)
[*] (Fix) CI translation key audit — expand dynamic prefix exclusions
[*] (Fix) rumour field name mismatches + missing translation keys + guidance
[*] (Fix) remove duplicate JSON translation keys (CI failure)
[*] (Fix) wholesaler spawning + getAllVisible + sandbox gate messages (§48)
[*] (Fix) remove stale return false + end from PlayerState.removeFromWatchlist
[*] (Fix) comprehensive ModData safety audit — 6 files, 15+ unsafe operations
[*] (Fix) ConnectionManager sprite table nil crash + CI forward-ref guard
[*] (Fix) remove duplicate threshold tables from POS_Constants.lua
[*] (Fix) replace # operator on Java ModData with pairs() iteration
[*] (Fix) add 51 missing translation keys + deduplicate UI.json
[*] (Fix) ambient intel connection guard + trait translation keys
[*] (Fix) add missing ZONE_DEFAULT_VOLATILITY + PERK_SIGINT constants
[*] (Fix) remove duplicate UI_POS_Assignments_FilterActive translation key
[*] (Fix) notifyOrSay broken signature + register 5 PN channels
[*] (Fix) remove duplicate WholesalerDir_Title translation key
[*] update §32.4 Voice Packs — expand from 1 to 4 section types
[*] update roadmap §8 — screen consolidation complete
[*] update §33.1 screen tree for consolidation (25→22 screens)
[*] reframe Free Agents as operations actor layer + MP ownership design
[*] add PhobosNotifications integration to Free Agent system doc
[*] comprehensive Free Agent system architecture document
[*] comprehensive design doc update — fix stale status + add §45-46
[*] update roadmap — nearly complete
[*] mark Tier 2 roadmap items as done
[*] eliminate 27 magic values across 8 files
[*] split recipes + complete callback delegation + luacheckrc
[*] recipe callback helpers + strip diagnostics + docs §29
[/list]

[hr][/hr]

[h2]v0.20.0[/h2]
[list]
[*] band registry + band-gated mission visibility
[*] redesign Assignments screen — dual tab bars (category × status)
[*] market event system — POS_EventService + probability-based firing
[*] mission content expansion — 10 new definitions across 6 categories
[*] Living Market Phase 2-3 — zone agent population + 4 voice packs
[*] integrate PhobosLib street data across all location displays
[*] add POS_TerminalStation entity for computer-based crafting
[*] (Fix) use percentage-based panel widths in initialise() too
[*] (Fix) contacts keys + percentage-based side panels (20% each)
[*] (Fix) translation key bleedthrough + terminal size 75% default
[*] (Fix) add connected=true to all hub menu contexts
[*] (Fix) revert Starlit TaskManager — KahluaArray string key crash
[*] (Fix) workaround Starlit TaskManager offset nil crash
[*] (Fix) remove duplicate translation keys (recovery, survey)
[*] (Fix) defer popup registrations to OnGameStart (load order crash)
[*] add §40.7 Starlit TaskManager offset workaround
[*] update §40 Starlit status — Phase 2 (TaskManager) adopted
[*] mark Tier 1 roadmap items as done
[*] add expansion roadmap — 14 features across 3 priority tiers
[*] replace manual tick counters with Starlit TaskManager
[*] remove all SIGINT screen gates — skill affects quality, not access
[*] move Contracts under BBS, replace tabs with unified list + badges
[/list]

[hr][/hr]

[h2]v0.19.0[/h2]
[list]
[*] contract screen + ContextPanel detail + design docs §42-§43
[*] sell-side contract system — schema, service, generator, text pools (§43)
[*] inline buy controls on CommodityItems screen
[*] (Fix) string.format crash on buy notification — PZ getText vs Lua format
[*] (Fix) replace invalid timedAction 'Craft' with 'Making_Surface'
[*] (Fix) Starlit import path + rewrite all recipes to B42 craftRecipe format
[/list]

[hr][/hr]

[h2]v0.18.0[/h2]
[list]
[*] Mission Text Compositor — data-driven briefing generation (§32)
[*] ContextPanel — add action + progress item types, fix bar builder ref
[*] implement SignalPanel — left sidebar passive intel feed (§9.2)
[*] Starlit LuaEvent integration — POS_Events namespace + emitters
[*] apocalypse-adjusted item pricing with curated value overrides
[*] dynamic boot sequence from definition files
[*] replace CRT bezel with telnet-style header + status bar frame
[*] data reset developer tool — wipe all POSnet data from save
[*] item pool curation — two-layer filter removes ~1,140 junk items
[*] informative empty state + discovery counter on View Items
[*] (Fix) remove duplicate UI_POS_Band_Tactical/Operations translation keys
[*] (Fix) remove stale NAV_PANEL_WIDTH reference from TerminalUI
[*] (Fix) DataReset modal button callbacks crash on self.parent nil
[*] (Fix) stale gmd variable in InvestmentResolver payout delivery
[*] (Fix) migrate all file I/O to world ModData — eliminate Zomboid/Lua/POSNET/
[*] (Fix) render crash from missing border alpha + data reset nav + remove nav panel
[*] (Fix) Data Reset screen not appearing — wrong callback name
[*] (Fix) ItemPool crash from load-order constant capture + lazyInit no-retry
[*] (Fix) AmbientIntel init never registered EveryOneMinute handler
[*] (Fix) View Items empty state, dead buttons, Market Overview overflow
[*] restructure into guides/ + architecture/ for audience separation
[*] update §9, §32, §40 status banners to reflect implementation
[*] comprehensive audit remediation — §9 rewrite, §33 update, §41 new
[*] add §40 Starlit Library integration roadmap
[*] update design guidelines for telnet frame + boot sequence
[*] document scroll panel pattern, dual-population, button redirects
[*] consolidate documentation — merge 3 files, inline 2 tables
[*] add duplicate JSON key detection for all translation files
[*] replace all ModData.getOrCreate with PhobosLib world ModData helpers
[*] update theme, constants, and widgets for telnet frame
[/list]

[hr][/hr]

[h2]v0.17.0 — Screen Consolidation & Item Discovery[/h2]
[list]
[*] item discovery via ambient intel — progressive trade catalog
[*] ambient market intelligence — passive "word of mouth" data
[*] inline trade confirm + watchlist history button
[*] consolidate terminal screens (20 → 12 navigable)
[*] (Fix) add missing translation keys for MarketOverview + Contacts
[*] (Fix) SIGINT skill registration via declarative perks.txt
[*] add mission system design doc + §32 Mission Content System
[*] remove 39 unused/always-on sandbox options (135 → 96)
[/list]

[hr][/hr]

[h2]v0.16.0[/h2]
[list]
[*] standalone trading system + DT removal
[*] (Fix) CI constants check scans all POS_Constants*.lua files
[*] (Fix) wrap MarketFileStore read loop in safecall for corruption resilience
[*] (Fix) move all file I/O to POSNET/ subdirectory to prevent Kahlua CTD
[*] (Fix) split POS_Constants.lua into 6 domain files to fix Kahlua crash
[*] (Fix) move confidence threshold tables after base constant definitions
[*] (Fix) registerScreen gracefully skips nil fields during ResetLua
[*] (Fix) add missing TRADE_STOCK_THRESHOLDS and constant aliases
[*] add §30-31 trading/compositor rules, update interop matrix
[*] migrate MarketFileStore from file I/O to world ModData
[*] split recipes + complete callback delegation + luacheckrc
[*] recipe callback helpers + strip diagnostics + docs §29
[/list]

[hr][/hr]

[h2]v0.15.0 — POSnet v0.15.0[/h2]
[list]
[*] configurable economy tick interval (1-24 hours, default 24)
[*] Living Market Phases 4, 5C, 6, 7 — complete implementation
[*] Phase 3B — soft signal rumour emission system
[*] PriceEngine zone pressure integration (Phase 3D)
[*] satellite-to-terminal physical wiring connection
[*] Living Market Phases 2, 5B, 3A — archetypes, zone persistence, signal emission
[*] Media Management sub-menu with auto-feed and family-grouped insertion
[*] add terminal link + fuel gates to satellite context menu
[*] grid power detection, VHS conversion, note improvements
[*] staggered market file saves via PhobosLib ChunkedWriter
[*] activate Living Market Phase 5.75 tick hook
[*] implement tick orchestration and zone pressure
[*] implement full wholesaler lifecycle
[*] add 8 wholesaler definitions and loader
[*] add Living Market simulation tuning constants
[*] (Fix) replace next() with pairs() count for ModData table emptiness check
[*] (Fix) add granular breadcrumbs + safecall migration registry lookup
[*] (Fix) wrap _updateAgentMeters in safecall + add phase breadcrumbs
[*] (Fix) zone overview uses theme colours instead of raw RGBA arrays
[*] (Fix) add missing archetype paths + fix safecall return destructuring
[*] (Fix) refactor economy tick to use monotonic getWorldAgeHours()
[*] (Fix) add sanity reset for corrupted lastProcessedTick in economy tick
[*] (Fix) wrap distributions in OnPreDistributionMerge event handler
[*] (Fix) add missing SIGINT_XP_SATELLITE_CALIBRATE constant
[*] (Fix) replace nonexistent MARKET_CATEGORIES with MarketRegistry lookup
[*] (Fix) move server/ modules to shared/ for SP compatibility
[*] (Fix) make POS_RumourGenerator a global (not local) for PZ module system
[*] (Fix) add missing require for POS_RumourGenerator in BBS screens
[*] (Fix) use correct PZ perk name 'Electricity' instead of 'Electrical'
[*] (Fix) commodities screen renders categories even with no market data
[*] (Fix) satellite calibration persists for outdoor dishes
[*] (Fix) satellite hasTerminalLink uses sprite lookup instead of keywords
[*] (Fix) strip Item_ prefix from all 35 icon fields to fix question marks
[*] (Fix) pre-warm PlayerFileStore cache in RadioInterception OnGameStart
[*] (Fix) revert broken cache warm + add missing textBright fallback colour
[*] (Fix) defer PlayerFileStore cache warm to first EveryOneMinute tick
[*] (Fix) pre-warm PlayerFileStore cache on game start to prevent CTD
[*] (Fix) getSummary returns nil instead of empty table when no records exist
[*] (Fix) add missing aggregate field to file store category tables
[*] (Fix) populate satellite dish and camera workstation sprite tables
[*] (Fix) add missing Living Market simulation constants
[*] (Fix) eliminate all sendServerCommand calls in singleplayer
[*] (Fix) isAuthority() incorrectly returned false in singleplayer
[*] (Fix) eliminate CTD from bulk observation data in market snapshot
[*] (Fix) skip market snapshot processing during early init
[*] (Fix) forward-declare ensureInit to satisfy luacheck
[*] (Fix) guard watchlist refresh against frame-0 init crash
[*] (Fix) correct SkillRequired syntax in craftRecipe definitions
[*] (Fix) wire watchlist refresh to economy tick and flush file store on save
[*] expand §28 with Seven Questions + call discipline rules + screen refresh triggers
[*] add §28 Interoperability Principles + interop matrix
[*] mark all Living Market phases complete, add §24.12-13
[*] mark Phase 3B complete, add §24.11 Soft Signal Rules
[*] mark Phase 3D complete, add zone pressure bias rule to §24.10
[*] update design-guidelines §24.10 and implementation status for Phases 2/5B/3A
[*] add Living Market implementation status and roadmap
[*] remove 6 stale POS_PlayerFileStore references
[*] add §27.6 Per-Player Data Storage convention
[*] add §25.6 Empty-Data Return Convention
[*] add §27.5 SP-Safe Server Commands to design guidelines
[*] add §27.4 Chunked File Writes to design guidelines
[*] add §27 Init-Time Performance guidelines
[*] update Living Market docs for Phase 1 implementation
[*] add POS_Constants field existence check
[*] harmony audit — pcall migration, translation keys, nil guards
[*] migrate watchlist+alerts from file store to player modData
[*] strip diagnostic breadcrumbs from Markets and Stockmarket screens
[*] dump first summary values + narrow crash to coverage section
[*] granular A-J breadcrumbs in IntelSummary create()
[*] remove early-return, enable full IntelSummary create with breadcrumb
[*] move diagnostic to IntelSummary screen, revert markets hub
[*] diagnostic — render menu entries only, skip upload section
[*] add early-return diagnostic to markets and stockmarket screens
[*] add refresh/getContextData diagnostic breadcrumbs
[*] add diagnostic breadcrumbs to markets screen create()
[*] defer init-time operations to reduce frame-0 resource spike
[*] bump POSnet to v0.15.0 — Living Market Phase 1
[*] diag: wrap PlayerFileStore warm in safecall to capture error
[/list]

[hr][/hr]

[h2]v0.14.0[/h2]
[list]
[*] data-pack architecture with schema validation and registries (v0.14.0)
[*] expand data-pack architecture guidelines from design review
[/list]

[hr][/hr]

[h2]v0.13.0 — Living Market Scaffolding & Defensive pcall Migration[/h2]
[list]
[*] Living Market Layer 0 scaffolding — autonomous economy framework with agent archetypes, wholesaler system, market zones, and supply pressure modeling (gated by sandbox option, not yet active)
[*] 3 new shared modules: POS_MarketAgent, POS_WholesalerService, POS_MarketSimulation
[*] Comprehensive Living Market constants — 7 agent archetypes, 6 market zones, 6 market event types, 6 wholesaler operational states
[*] ~37 POSnet defensive pcalls migrated to PhobosLib.safecall for Strict Mode compatibility
[*] Fixed logic error in POS_TutorialService.migrateLegacy() (line 205)
[*] Replaced local math utilities with PhobosLib equivalents (clamp, lerp, randFloat)
[*] Updated design guidelines with Living Market module references and Strict Mode pcall patterns
[*] PhobosLib 1.48.0 dependency (new math/table utilities)
[*] Full translation infrastructure for Living Market entities (agents, zones, events, states, signals)
[/list]

[h2]v0.10.0 — Danger Detection, Cache Externalization & Polish (Preview)[/h2]
[list]
[*] Danger detection gate — intel gathering and passive recon blocked when threats nearby (zombies, fire, combat)
[*] 5-state context menu for intel gathering (ready/wrong location/danger/missing items/cooldown)
[*] Building and mailbox caches externalized to flat files (prevents ModData bloat)
[*] Auto-migration from old ModData caches on first load
[*] Unique Raw Market Note icon (folded paper with field notes aesthetic)
[*] Dynamic tooltips via PhobosLib.registerTooltipProvider() — notes show category, items, prices
[*] Journal/document writing system for in-game readable intel
[*] Item name filtering for display names (removes "Base." prefix)
[*] Iteration history document (ITERATION_HISTORY.md)
[*] DangerCheckRadius sandbox option (default 15, range 5-30)
[*] Tooltip.json created (fixes raw key display in item tooltips)
[*] PhobosLib 1.40.0 dependency (danger detection, document writing, tooltip providers)
[/list]

[h2]v0.9.0 — Market Intelligence & Passive Recon (Preview)[/h2]
[list]
[*] Scanner radio passive recon — all 12 vanilla radios enhanced with 4-tier system
[*] Terminal UI overflow guards and text truncation
[*] Exit button on root screens, Back button consistency
[*] Comprehensive loot table expansion (17 → 44 distribution entries)
[*] Critical fix: sendServerCommand SP compatibility
[*] Critical fix: weightFn missing in item selection
[*] Sandbox tooltip %% display fix
[*] PhobosLib 1.38.0 dependency (text truncation utilities)
[/list]

[h2]v0.8.0 — Persistence Migration & VHS Tapes[/h2]
[list]
[*] World-scoped Global ModData persistence (Phase A-E migration)
[*] Server-authoritative economy with daily tick
[*] Passive recon devices: camcorder, field logger, data calculator
[*] VHS-C tapes: 4 quality tiers, crafting, degradation, recycling
[*] VHS tape review at TV CraftBench entity
[*] Intel gathering cooldown (per-location, 12-day default)
[*] 4-state context menu for intel gathering
[*] Dynamic note tooltip showing category-specific items
[*] Append-only event logs for market auditing
[*] 11 item icons generated via gpt-image-1
[*] Client snapshot protocol for multiplayer
[*] Server input validation and admin commands
[/list]

[h2]v0.7.0 — Market Intelligence Engine[/h2]
[list]
[*] Item pool system (5,105 vanilla items mapped to 11 commodity categories)
[*] Price engine with day-to-day drift, reputation-scaled variance, speculation
[*] 18 sub-categories with weighted selection
[*] 13 sandbox options for market weighting and category control
[*] PhobosLib.weightedRandom() and weightedRandomMultiple() utilities
[/list]

[h2]v0.6.0 — Code Quality Audit[/h2]
[list]
[*] POS_Constants.lua centralised (24 cross-file constants)
[*] 35 files refactored — magic numbers and strings extracted
[*] 5 new sandbox options (delivery expiry, scan radii, investment variance)
[*] COLOURS palette delegation to POS_TerminalTheme
[/list]

[h2]v0.5.0 — Screen Stack & Terminal Layout[/h2]
[list]
[*] 3-column terminal layout (NavPanel, ContentPanel, ContextPanel)
[*] Screen stack architecture with API, registry, and guard system
[*] Terminal power consumption (generator fuel drain)
[*] AZAS Frequency Index integration (dual-band registration)
[*] Signal strength system (inverse square law from TransmitRange)
[*] CRT bezel texture and 4 colour themes
[/list]

[h2]v0.4.0 — BBS & Investments[/h2]
[list]
[*] BBS Hub with investments, operations, and courier sections
[*] Investment system with risk/reward simulation
[*] Mission negotiation and cancellation
[*] Pagination support via PhobosLib_Pagination
[*] Street address resolution via Paper Trails
[/list]

[h2]v0.3.0 — Boot Sequence & Resizable Windows[/h2]

[list]
[*] DOS-style boot animation with typewriter character reveal on first terminal open per world-load
[*] Terminal window now resizable via drag handle (compatible with "Resize Any Window" mod)
[*] Click anywhere during boot to skip to operations view
[*] Boot animation speed scales with game speed multiplier
[/list]

[h2]v0.2.0 — Core Gameplay Loop & Terminal UI[/h2]

[list]
[*] Retro CRT-style terminal UI — green monospace text on dark background with scanline effect
[*] Physical setup: right-click any radio near a vanilla desktop computer to connect
[*] "Connect to POSnet" context menu on world-placed and inventory radios
[*] Connection validation: checks radio power, on/off state, and desktop computer proximity
[*] Server-to-client operation delivery via OnServerCommand
[*] 5 built-in starter missions
[*] Welcome guide popup explaining setup and usage
[/list]

[h2]v0.1.0 — Initial Scaffold[/h2]

[list]
[*] POSnet radio channel registration at configurable frequency
[*] Procedural mission generation from registered templates
[*] 5 mission categories
[*] 4 difficulty levels: Easy, Medium, Hard, Critical
[*] Extensible completion detector with item_acquire built-in checker
[*] Player-side operation log with persistence in modData
[*] Server-side timed broadcast system
[*] 6 sandbox options
[*] CI pipeline with 9 automated checks
[/list]
